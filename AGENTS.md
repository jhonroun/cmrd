Данный проект предназначен для переписывания другого проекта на идиоматический go.

задача: нужно взять файл из reference/cloude_mail_downloader.php полностью переписать для ражеимов работы: TUI через BUBLETEA, обновляемый интерфейс, отображения прогремма, обновление экрана, а не перезапись. aria2c берем из переменных окружения. должен работать как на windows, так и на linux. также нужен grpc интерфейс, для того чтобы написать WEBUI или другой gui. 

также нужно иметь возможность не просто компилировать проект, а подключать как библиотеку.

нужен полноценный help в приложении и в папке doc подробная документация проекта на русском и английском языке. Тип лицензии наследуется от aira2c. файл README.md а корне должен содержать ссылки на документацию для обоих языков, а также саммари на обоих языках, включая quik start и first run для обоих языков.

все шаги нужно писать в changelog.md в формате 
mm.dd.yyyy hh:mm
что сделано и для чего коротко в одном предложении.

Сначала планируем, т.е. детально и подробно описываем план реализации в фале todo.md (на русском), а потом поэтапно проверяем. код должен тестироваться, проходить проверки линтером. В проекте есть git система. коммиты должны быть атомарными, одна фича - один коммит, с пояснением что сделано, а не почему.